# Day 20: 检索优化：Hybrid Search

**日期**：2026-05-10

## 今天做了什么

纯向量检索对精确匹配支持不够。比如搜索「Python 3.13」，向量检索可能搜到「Python 性能优化」而不是精确的版本。Hybrid Search 结合了向量和关键词检索（BM25），效果提升明显。

用简单的方式实现了：搜索结果两边各取 top-k，然后合并去重。

## 明天计划
